spring:
  application:
    name: userservice
  
  # ✅ Exclude Spring Cloud DataSource auto-config
  autoconfigure:
    exclude:
      - org.springframework.cloud.autoconfigure.RefreshAutoConfiguration
  
  datasource:
    url: jdbc:mysql://localhost:3306/clothing_platform?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: u8re4cvm
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

server:
  port: 8081

# JWT Configuration
jwt:
  secret: bXktc3VwZXItc2VjcmV0LWtleS1mb3ItanNvbnN3ZWJ0b2tlbnM=
  expiration: 86400000  # 24 hours in milliseconds

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.clothing.userservice: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.boot.autoconfigure: DEBUG