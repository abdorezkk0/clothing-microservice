server:
  port: 8082

spring:
  application:
    name: productservice
  
  datasource:
    url: jdbc:mysql://localhost:3306/clothing_platform_products?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: u8re4cvm
  
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# ✅ ADD JWT CONFIGURATION
jwt:
  secret: bXktc3VwZXItc2VjcmV0LWtleS1mb3ItanNvbnN3ZWJ0b2tlbnM=
  expiration: 86400000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    hostname: localhost
    prefer-ip-address: false
    instance-id: ${spring.application.name}:${server.port}

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
  security:
    enabled: false

logging:
  level:
    org.springframework.web: DEBUG
    com.clothing.productservice: DEBUG
    org.hibernate.SQL: DEBUG